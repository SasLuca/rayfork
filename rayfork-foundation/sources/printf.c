#if 0
#include "varargs.h"

rf_strbuf rf_format_str(rf_str str, ...)
{
    rf_strbuf result = rf_strbuf_make(1024, rf_default_allocator);

    while (rf_str_valid(str))
    {

    }

    return result;
}
#endif